pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Building dev branch"'
            }
        }
        stage('Test') {
            steps {
                sh 'echo "Running tests on dev branch"'
            }
        }
    }
}
